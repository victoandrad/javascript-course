// Object.getOwnPropertyDescriptors(objeto)
// método usado para recuperar o objeto de configuração de todos os atributos

const pessoa = {
    nome: "Luiza Aurora"
}

console.log(Object.getOwnPropertyDescriptors(pessoa))