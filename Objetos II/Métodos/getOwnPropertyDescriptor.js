// Object.getOwnPropertyDescriptor(objeto, "atributo")
// método usado para recuperar o objeto de configuração do atributo

const pessoa = {
    nome: "Luiza Aurora"
}

console.log(Object.getOwnPropertyDescriptor(pessoa, "nome"))