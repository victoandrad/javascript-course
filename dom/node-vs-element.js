// node vs element

// there are 12 different types of nodes in the HTML DOM (element, attribute, text, comment, etc)
// an element is just a specific type of node

// the NodeList is a static structure.
// the HTMLCollection is a dynamic structure.