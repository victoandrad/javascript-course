// splice
// removes and returns a piece of the array according to the indicated indices
// adds new elements passed by parameter

let arr1 = [1, 2, 3, 5, 6, 7, 8, 9, 10]

// starts removing at index 2, remove 4 elements, add new elements
let arr2 = arr.splice(2, 4, 'Luiza Aurora')

console.log(arr1)
console.log(arr2)