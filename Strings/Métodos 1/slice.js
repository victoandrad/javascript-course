// slice()
// Método usado para recortar uma parte da string
// Diferente do substring(), o slice() aceita valores negativos

const nome = "Luiza Aurora"
console.log(nome.slice(2, 5))
console.log(nome.slice(-5, -2))

