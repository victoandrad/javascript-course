// padEnd()
// Método usado para adicionar um espaçamento no final da String
// Recomendado para padronizar tamanho de Strings

const nome = "Luiza Aurora"
console.log(nome.padEnd(20))
console.log(nome.padEnd(20, "*"))
