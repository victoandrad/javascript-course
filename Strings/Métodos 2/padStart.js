// padStart()
// Método usado para adicionar um espaçamento no começo da String
// Recomendado para padronizar tamanho de Strings

const nome = "Luiza Aurora"
console.log(nome.padStart(20))
console.log(nome.padStart(20, "*"))
