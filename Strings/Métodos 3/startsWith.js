// startsWith()
// Método usado para verificar se começa com a sequência informada

const nome = "Luiza Aurora"
console.log(nome.startsWith("Lu"))

// Pode receber um parâmetro opcional de qual índice começar
console.log(nome.startsWith("Lu", 1))
